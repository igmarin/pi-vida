# Copy template — replace `model:`/`thinking:` per slot for your providers.
# Copy into a target repo (e.g. .pi/fusion-harness/model-stack-trio.yaml)
# and pass its path: `pi-vida <vida> fusion <path>` (or pi --fh-config).
- name: fable
  model: anthropic/claude-fable-5
  thinking: medium
  architect: true
  color: "#A78BFA"

- name: sol
  model: openai/gpt-5.6-sol
  thinking: medium
  primary: true
  color: "#F59E0B"

- name: terra
  model: openai/gpt-5.6-terra
  thinking: medium
  color: "#22D3EE"
